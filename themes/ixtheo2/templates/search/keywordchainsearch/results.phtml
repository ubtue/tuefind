<?php
$this->headTitle($this->translate('Browse Keywordchains'));
$this->layout()->breadcrumbs = '<li class="breadcrumb-item active"><a href="' . $this->url('keywordchainsearch-home') . '">' . $this->transEsc('Browse Keywordchains') . '</a></li>';

$lookfor = '';
if (isset($this->results)) {
    $lookfor = $this->results->getUrlQuery()->isQuerySuppressed() ? '' : $this->params->getDisplayQuery();
}
?>
<?= $this->context($this)->renderInContext(
    'search/keywordchainsearch/searchbox.phtml',
    [
        'lookfor' => $lookfor,
    ]
); ?>

<?= $this->render('search/keywordchainsearch/result-list.phtml') ?>
<?= $this->context($this)->renderInContext('search/bulk-action-buttons.phtml', ['idPrefix' => 'bottom_']) ?>
<?= $this->paginationControl($this->results->getPaginator(), 'Sliding', 'Helpers/pagination.phtml', ['results' => $this->results]) ?>






