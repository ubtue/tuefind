<?php $entries = array_map(function ($entry) { ?>
  <?php list($ppn, $preceding_title) = $entry; ?>
  <?php return '<a href="' . $this->recordLinker()->getUrl($ppn) . '">' . htmlspecialchars($preceding_title) . '</a>'; ?>
  <?php }, $data ?? []);?>
<?=implode('</br>', $entries);?>
