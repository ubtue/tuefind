<?php
// Set up page title:
$this->headTitle($this->translate('Admin History'));

// Set up breadcrumbs:
$this->layout()->breadcrumbs = '<li><a href="' .  $this->url('myresearch-home') . '">' . $this->transEsc('Your Account') . '</a></li> <li class="active">' . $this->transEsc('Admin history') . '</li>';
?>

<div class="<?=$this->layoutClass('mainbody')?>">
  <h2>Admin history</h2>
  <?php if (count($this->history_datas) == 0): ?>
    <p>No history.</p>
  <?php else: ?>
    <table class="table dataTable display">
      <thead>
        <tr>
          <th><?=$this->transEsc('Admin Name')?></th>
          <th><?=$this->transEsc('Request User')?></th>
          <th><?=$this->transEsc('Authority')?></th>
          <th><?=$this->transEsc('Access')?></th>
          <th><?=$this->transEsc('Request User Date')?></th>
          <th><?=$this->transEsc('Admin request Date')?></th>
        </tr>
      </thead>
      <tbody>
        <?php foreach ($this->history_datas as $data): ?>
          <tr>
            <td><?=$data->admin_username." (".$data->admin_firstname." ".$data->admin_lastname.")"?></td>
            <td><?=$data->request_user_firstname." ".$data->request_user_lastname?></td>
            <td><a href="/Authority/<?=$data->authority_id?>"><?=$data->authority_id?></a></td>
            <td><?=$data->access_type?></td>
            <td><?=$data->request_user_date?></td>
            <td><?=$data->admin_request_date?></td>
        <?php endforeach; ?>
      </tbody>
    </table>
  <?php endif; ?>
</div>

<div class="<?=$this->layoutClass('sidebar')?>" id="myresearch-sidebar">
  <?=$this->context($this)->renderInContext("myresearch/menu.phtml", ['active' => 'showadminhistory'])?>
</div>
